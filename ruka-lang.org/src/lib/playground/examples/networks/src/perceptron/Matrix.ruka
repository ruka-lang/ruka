let t = record {
	local buf: [float]
	rows: int
	cols: int
}

let new = (rows, cols) do {
	buf = { ruka.random(-1.0..=1.0) for 0..rows * cols }
	rows
	cols
}

let multiply (self) = (other) do

end

let add (self) = (other) do

end

let dot (self) = (other) do

end

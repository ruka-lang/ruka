// expect: cannot capture @-mode
// An `@`-mode declaration cannot be captured by a closure that outlives
// the declaring function.
let make = () do
	let @n = 0
	() do
		n
	end
end

let main = () do
	let f = make()
	ruka.println("${f()}")
end

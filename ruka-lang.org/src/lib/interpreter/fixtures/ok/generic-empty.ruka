// `~#t: type` — named compile-time type parameter, inferred from result location.
let empty = (~#t: type) -> [t] do [t]{}

let main = () do
	let xs: [i32] = empty()
	let ys: [string] = empty()

	ruka.println("xs len: ${xs.len()}")
	ruka.println("ys len: ${ys.len()}")
end

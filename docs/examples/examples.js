/* ── Playground examples ────────────────────────────────────────────
   Edit this file to add or change playground examples.
   Each key must match an <option value="..."> in index.html.
   One array entry = one line of code.
   ──────────────────────────────────────────────────────────────── */
window.RUKA_EXAMPLES = {
	'hello-world': [
		'share main = () do',
		'    let name = "world"',
		'    #.println("Hello, ${name}!")',
		'    #.println("1 + 2 = ${1 + 2}")',
		'end',
	].join('\n'),

	'calculator': [
		'local add = (a, b) do a + b',
		'local sub = (a, b) do a - b',
		'local mul = (a, b) do a * b',
		'local div = (a, b) do a / b',
		'',
		'share main = () do',
		'    let x = 12',
		'    let y = 4',
		'    #.println("${x} + ${y} = ${add(x, y)}")',
		'    #.println("${x} - ${y} = ${sub(x, y)}")',
		'    #.println("${x} * ${y} = ${mul(x, y)}")',
		'    #.println("${x} / ${y} = ${div(x, y)}")',
		'end',
	].join('\n'),

	'fibonacci': [
		'local fib = (n) do',
		'    n if n <= 1 else fib(n - 1) + fib(n - 2)',
		'end',
		'',
		'share main = () do',
		'    let i = 0',
		'    while i <= 10 do',
		'        #.println("fib(${i}) = ${fib(i)}")',
		'        i = i + 1',
		'    end',
		'end',
	].join('\n'),

	'fizzbuzz': [
		'share main = () do',
		'    let i = 1',
		'    while i <= 20 do',
		'        if i % 15 == 0 do #.println("FizzBuzz")',
		'        else if i % 3 == 0 do #.println("Fizz")',
		'        else if i % 5 == 0 do #.println("Buzz")',
		'        else #.println("${i}")',
		'        i = i + 1',
		'    end',
		'end',
	].join('\n'),

	'arrays': [
		'share main = () do',
		'    let primes = [int].{2, 3, 5, 7, 11}',
		'    #.println("primes:  ${primes}")',
		'    #.println("length:  ${primes.length()}")',
		'    #.println("first:   ${primes[0]}")',
		'    #.println("last:    ${primes[primes.length() - 1]}")',
		'',
		'    primes.append(13)',
		'    #.println("after append(13): ${primes}")',
		'',
		'    primes.remove(0)',
		'    #.println("after remove(0):  ${primes}")',
		'',
		'    // Iterate with for',
		'    let sum = 0',
		'    for p in primes do sum = sum + p',
		'    #.println("sum:     ${sum}")',
		'',
		'    // Slice with a range',
		'    #.println("slice:   ${primes[1..4]}")',
		'end',
	].join('\n'),

	'ranges-and-chars': [
		'share main = () do',
		'    // Numeric range — i is bound per iteration',
		'    for i in 1..=5 do',
		'        #.println("${i} ** 2 = ${i ** 2}")',
		'    end',
		'',
		'    // Char range — iterate letters directly',
		'    let word = ""',
		'    for c in \'R\'..=\'Z\' do word = word.concat("${c}")',
		'    #.println("letters: ${word}")',
		'',
		'    // String indexing — returns a u8 that prints as its character',
		'    let name = "Ruka"',
		'    #.println("first:   ${name[0]}")',
		'    #.println("slice:   ${name[1..=3]}")',
		'',
		'    // break & continue',
		'    for i in 0..20 do',
		'        if i % 2 == 0 do continue',
		'        if i > 9       do break',
		'        #.println("odd ${i}")',
		'    end',
		'end',
	].join('\n'),
};

let add = (a, b) do a + b
let sub = (a, b) do a - b
let mul = (a, b) do a * b
let div = (a, b) do a / b

let main = () do
    let x = 12
    let y = 4
    ruka.println("${x} + ${y} = ${add(x, y)}")
    ruka.println("${x} - ${y} = ${sub(x, y)}")
    ruka.println("${x} * ${y} = ${mul(x, y)}")
    ruka.println("${x} / ${y} = ${div(x, y)}")
end

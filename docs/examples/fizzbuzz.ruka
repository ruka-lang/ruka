share main = () do
    let i = 1
    while i <= 20 do
        if i % 15 == 0 do #.println("FizzBuzz")
        else if i % 3 == 0 do #.println("Fizz")
        else if i % 5 == 0 do #.println("Buzz")
        else #.println("${i}")
        i = i + 1
    end
end

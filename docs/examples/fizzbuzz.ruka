share main = () do
    let i = 1
    while i <= 20 do
        if i % 15 == 0 => ruka.println("FizzBuzz")
        else if i % 3 == 0 => ruka.println("Fizz")
        else if i % 5 == 0 => ruka.println("Buzz")
        else => ruka.println("${i}")
        i = i + 1
    end
end
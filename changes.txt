- Reverted to `ruka.` for builtins. I think I updated the docs and examples completely, playground and highlighter need updating to this change
- Privacy like golang but the opposite: `Ident` = private, `ident` = public. Docs and playground/highlighter need updating
- Run-time bindings can make use of the & (mov) mode ensuring if they are captured by a closure their ownership is moved into that closure and they can no longer be used outside of it. Docs need updating

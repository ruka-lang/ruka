const std = rex.import("std");
const fmt = std.fmt;

/* Program to print Hello, world! to the terminal */
const main = () => int {
    let name = "world";
    let msg = name
        |> fmt.sprintf(format: "Hello, {}!");

    std.io.println(msg);

    return 0;
}
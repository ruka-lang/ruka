const std = @import("std");

let name = "Foo";
let year = 2023;

# Formats greeting with name and year
$type(fn (string, isize) -> string)
const greet = (greeting, name, year) {
    return std/fmt.string("Hello, {s}! From {d}", name, year);
};

greet(greeting, name, year) 
    |> std/io.println();

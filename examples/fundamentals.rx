const std = @import("std");

let name = "Foo";
let year = 2023;
let multi = "| Hello,
             | {s}!
             | From {d}
             |"

# Formats greeting with name and year
const greet = (name: string, year: int) -> string |&multi| {
    return std/fmt.string(multi, name, year);
};

greet(greeting, name, year) 
    |> std/io.println();

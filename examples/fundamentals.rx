const std = @import("std");

let name = "Foo";
let year = 2023;
let multi = "| Hello,
             | {s}!
             | From {d}
             |"

# Formats greeting with name and year
$type(cl (string, int) -> string)
const greet = (greeting, name, year) |multi| {
    return std/fmt.string(multi, name, year);
};

greet(greeting, name, year) 
    |> std/io.println();

/*
 *
 */
const project = {
    name = "basics",
    version = "0.0.1",
    ruka_version = "0.0.0"
}

/*
 *
 */
const executable = {
    name = project.name,
    path = "src/main.ruka"
}

/*
 *
 */
const library = {
    name = project.name,
    path = "src/root.ruka"
}

/*
 *
 */
const dependencies = {
    std = { version = project.ruka_version, features = [] }
}

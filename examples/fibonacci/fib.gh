let mut cache = [50]{0, 1, 1, -1, ...};

/* Anonymous closure */
pub const fibonacci = (n) { |&|
    if n < 0 => return 0;

    if cache[n] != -1 {
        return cache[n];
    } else {
        cache[n] = fibonacci(n-1) + fibonacci(n-2);
        return cache[n];
    }
}

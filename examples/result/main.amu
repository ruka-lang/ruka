use std;

fn Result -> typeid = (
    @success: typeid,
    @failure: typeid
) do 
    return union {
        Ok(success),
        Err(failure)
    };
end

pub fn main -> Result((), any) = () do
    std.io.println("Hello, world!"); 

    Ok(())
end

/*
 * Program to calculate fibonacci numbers
 */

const std = warp.import("std")
const fib = warp.import("fib")

use std.string

const main -> Result(unit) = () {
    let n = gets()
        |> string.parse() ~into: int 
        |> (?)

    let value = fib.fibonacci(n)

    println("Fibonacci number {} is {}", {n, value})

    () |> Ok
}

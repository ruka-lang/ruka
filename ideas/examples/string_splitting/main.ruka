/!*
  * Program to split a string
  * @author: ruka-lang
  */

const std = @import("std")

/** Takes a string and splits the string into substrings at each occurence of
  * the passed in character
  *
  * # Examples
  * ```
  * @> strsplit("Hello, world!", ' ')
  * ["Hello,", "world!"]
  * ```
  */
fn strsplit -> std.ArrayList(string) = (
    str: string,
    ch: u8
) {
    var indices = std.ArrayList(uint).new()
    for c, i in str, 0.. {
        if c == ch => indices.push(i)
    }

    var words = std.ArrayList(string).new()
    var prev = 0

    for idx in indices {
        let str = str[prev..idx].into(string)
        words.push(str)
        prev = idx + 1
    }

    let str = str[prev..str.len].into(string)
    words.push(str)

    words
}

fn main -> !unit = () {
    let words =
        "| Hello,
         | world!
         |"
        |> ([..])
        |> strsplit('\n')

    @assert(words.len == 3).!
}

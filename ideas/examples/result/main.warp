use std;

const Result -> typeid = (
    comptime success: type,
    comptime failure: type
) do 
    union {
        Ok(success),
        Err(failure)
    }
end

const main -> Result((), any) = () do
    std.io.println("Hello, world!"); 

    Ok(())
end

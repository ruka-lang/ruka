use std;

const Result -> type = (
    comptime success: type,
    comptime failure: type
) do 
    union {
        Ok(success),
        Err(failure)
    }
end

const main -> Result((), string) = () do
    std.io.println("Hello, world!"); 

    Ok(())
end

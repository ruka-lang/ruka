use std;

fn Result -> typeid = (
    comptime success: type,
    comptime failure: type
) do 
    return union {
        Ok(success),
        Err(failure)
    };
end

fn main -> Result((), any) = () do
    std.io.println("Hello, world!"); 

    Ok(())
end

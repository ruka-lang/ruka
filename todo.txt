TODO (Docs and playground)
- Char literal syntax highlighting
- Will need builtin functions to support manual heap allocation to allow for creating custom allocators that will support ruka.allocator behaviour
- If user exclusively uses manual memory management, GC will be excluded from binary
- In compile time, should be able to analyze a type's fields, methods, and static members and create a new type based on that (similar to rust derive macros)
- Better error checking
	- Using an identifier that hasn't been declared should be an editor error
	- String interpolation values are not checked for errors
- More playground features (in order of importance)
	- If needs to support multiline blocks using do...else if condition do...else...end
	- Builtin types, annotation and basic inference
	- Booleans
	- Chars
	- Tuples
	- Arrays
		- .append(item) method
		- .remove(index) method
		- .length() method
	- Ranges
	- String and Array indexing
	- Context aware indentation and autoclosing for () {} [] do end
	- Records
	- Variants
	- More fleshed out type inference
	- Patterns / Matching / Destructuring
	- Methods and statics
	- Modes
	- Behaviours
	- Builtin behaviours
	- More builtin functions
TODO (Features, Docs and playground)
- Documentation/Language features
	- Consider changing local back to static only, and let be immutable by default and requiring the mut mode `let *ident` to create a mutable variable mirroring the * parameter mode
		- Also removing local/share and using the compile-time mode to specify compile-time constants `let @ALPHA = ...` `let @main = ...`, @ is implied for methods and statics `let start (self) = ...`, and type definitions `let vec = record { ... }`, but I'm not sure how to handle specifying privacy cleanly (maybe `let local hash (self) = ...` for private and default to public since records use local before the ident for private fields). Bindings declared directly in a file (not enclosed in any kind of block) can implicitly be treated as @ so `let main` is treated as `let @main`. Explicit @ mode annotation only needed to force runtime code to execute during compilation when inside of blocks
    - Allowing ranges in match arm guards when matching integer-typed variables
	- Builtin types like arrays, tuples, strings, (maybe even primatives as well like integers) are treated like records and have methods
	- Methods are only appliable in the scope the type is defined in. Maybe this rule has an expection so you can add methods to foriegn or builtin type `let fold (self: [float]) = ...` but it only applies in that specific scope (this would be useful for adjusting a type to fit a behaviour, a new type is created with the new method and shadows the old type in that scope the method is defined in)
	- Variables and parameters can be shadowed but method/static overloading will never be supported
- More playground language support (in order of importance)
	- Concat should mutate, not return a new string
	- Playground input sanitization
	- Modes
	- Mutability/mode checks when mutating a binding or parameter
	- Records
	- Variants
	- Patterns / Matching / Destructuring
	- Methods and statics
	- Builtin functions (abs, sin, cos, tan, etc)
	- Behaviours
	- Builtin behaviours used by for loops and print functions
- Playground functionality
	- Make playground it's own page
	- User projects supporting multiple files, saved in local storage
	- Context aware indentation and autoclosing for `()` `{}` `[]` `""` `''` `do end`
        - do is only autoclosed with end if pressing enter right after it

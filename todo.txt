TODO (Features, Docs and playground)
- Documentation/Language features
    - Allowing ranges in match arm guards when matching integer-typed variables
	- Builtin types like arrays, tuples, strings, (maybe even primatives as well like integers) are records and have methods
	- Currently methods are only appliable in the scope the type is defined in. This rule should have an expection where you can add methods to foriegn or builtin type `let fold (self: [float]) = ...` but it only applies in that specific scope (this would be useful for adjusting a type to fit a behaviour, a new type is created with the new method and shadows the old type in that scope the method is defined in)
- More playground language support (in order of importance)
	- Playground input sanitization
	- Builtin functions (abs, sin, cos, tan, etc)
	- .concat for strings/arrays returns new string/array. .append mutates
	- Modes
	- Mutability/mode checks when mutating a binding or parameter
	- Records
	- Variants
	- Methods and statics
    - Array/String.concat returns new string, .append mutates the array/string
	- Patterns / Matching / Destructuring
	- Behaviours
	- Builtin behaviours used by for loops and print functions
- Playground functionality
	- Make playground it's own page
	- User projects supporting multiple files, saved in local storage
	- Context aware indentation and autoclosing for `()` `{}` `[]` `""` `''` `do end`
        - do is only autoclosed with end if pressing enter right after it
